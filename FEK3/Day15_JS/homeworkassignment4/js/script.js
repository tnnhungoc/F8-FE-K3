var a = 5;
var b = 0;
var sameSign = a * b;

if (sameSign >= 0) {
  console.log("they are same sign");
} else {
  console.log("they are not same sign");
}

// var sameSign = a * b >= 0 ? "they are same sign" : "they are not same sign";
// console.log(sameSign);
